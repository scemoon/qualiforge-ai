import React from 'react';
import type { StyledProps } from '../common';
import type { CalendarValue, TdCalendarProps } from './type';
export interface CalendarProps extends TdCalendarProps, StyledProps {
}
export interface CalendarMethods {
    /**
     * 显示当前月份\年份
     */
    toCurrent: (value: CalendarValue) => void;
}
declare const Calendar: React.ForwardRefExoticComponent<CalendarProps & React.RefAttributes<CalendarMethods>>;
export default Calendar;
