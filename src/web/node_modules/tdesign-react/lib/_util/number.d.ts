export declare const numberToPercent: (number: number) => string;
/**
 * 精确加法函数，避免 JavaScript 浮点误差
 * - 浮点运算会导致类似 0.1 + 0.2 = 0.30000000000000004 的问题
 * - 该函数通过将数字放大为整数进行运算，再缩小回原精度，得到更精确的结果
 */
export declare function accAdd(num1: number, num2: number): number;
